const prompt = require('prompt-sync')();

const num1 = prompt('What is your weight on Earth');
console.log(num1);

const num2 = Number(prompt('enter a relative gravity for the planet he wants to fight on'));

const weight = 150

if (weight * 0.78) {
   console.log(117);
   if (weight * 0.39) {
   console.log(58.5);
   }
   if (weight * 2.65) {
    console.log(397.5);
   }
   if (weight * 1.17) {
       console.log(175.5);
   }
   if (weight * 1.05) {
       console.log(157.5)
   }
   if (weight * 1.23) {
       console.log(184.50)
   }

}

